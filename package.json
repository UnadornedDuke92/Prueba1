{
  "name": "dental-creacion",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page — Dental Creación, consultorio dental (Acoxpa, CDMX)",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./css/styles.css --minify",
    "dev": "tailwindcss -i ./src/input.css -o ./css/styles.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
